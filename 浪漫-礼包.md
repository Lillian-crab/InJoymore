# 浪漫餐厅：等级礼包清单

## 口径与结论

这里的“等级礼包”指服务器配置 `levelupGift` 对应的付费升级礼包，不是 `LevelConfig.json` 中升级后自动领取的普通等级奖励。

截至目录内最新的服务器抓包 `20260728_215244`，`levelupGift` 配置为：

- 配置组：`groupID=23`，界面代码：`levelup`。
- 目标等级字段：`sLv=30`、`eLv=60`。
- 每次触发后有效 `3600` 秒，即 1 小时；`popups=1`，自动弹窗次数为 1 次。
- 礼包按序号 `index` 从 1 到 6 轮换，共 6 档；服务器 JSON 中的数组顺序不是序号顺序，客户端会按 `index` 排序。
- 每次升级事件都会尝试触发当前档礼包。只有购买成功后，客户端才把序号加 1；礼包过期、关闭或未购买时不会前进到下一档。购买完第 6 档后，下一次触发从第 1 档重新开始。

因此，不能简单地把“30级、31级、32级……”固定对应到第 1、2、3 档：实际档位取决于玩家是否买过前一档。`sLv/eLv` 是服务器下发的目标等级字段；客户端的升级事件代码没有直接读取这两个字段，实际是否下发/展示还受服务器配置和内购开关影响。

## 礼包档位（按实际 index 排序）

金额字段 `desc_price` 是服务器配置中的美元标价字符串，具体支付页面可能按地区显示本地货币。`giftID`、`payID` 保留原值，便于和抓包或日志核对。

| 档位 | 服务器 giftID | payID | 标价 | 固定内容（数量） | premium | discountTag 参数 |
|---|---|---|---:|---|---:|---:|
| 1 | `test2_3_1_1` | `backup199` | $1.99 | 宝石×100；体力×50；普通自选宝箱 `4101`×1；订单自选箱01-阶段1 `os_1_1`×1；资源礼包A01 `rc_a_1`×1；每日宝箱02 `3801`×1；存钱罐 `3101`×1；降阶碎片×1 | 3.7185929648 | 372 |
| 2 | `test2_3_1_2` | `cookiecoffee299` | $2.99 | 宝石×150；体力×100；普通自选宝箱 `4101`×1；普通自选箱03-阶段1 `ns_3_1`×1；资源礼包A01 `rc_a_1`×1；每日宝箱02 `3801`×1；存钱罐 `3101`×1；降阶碎片×1 | 3.5117056856 | 351 |
| 3 | `test2_3_1_3` | `cookiebread499` | $4.99 | 宝石×300；体力×100；普通自选宝箱 `4101`×1；普通自选箱03-阶段1 `ns_3_1`×1；资源礼包B01 `rc_b_1`×1；每日宝箱02 `3801`×1；存钱罐 `3101`×1；降阶碎片×1 | 2.8817635271 | 288 |
| 4 | `test2_3_1_4` | `cookiemonthlycard699` | $6.99 | 宝石×400；体力×200；普通自选箱02-阶段1 `ns_2_1`×1；普通自选箱04-阶段1 `ns_4_1`×1；资源礼包C01 `rc_c_1`×1；每日宝箱02 `3801`×1；存钱罐 `3101`×1；降阶碎片×1 | 3.0042918455 | 300 |
| 5 | `test2_3_1_5` | `cookieseashell799` | $7.99 | 宝石×450；体力×200；普通自选箱02-阶段1 `ns_2_1`×1；普通自选箱04-阶段1 `ns_4_1`×1；资源礼包D01 `rc_d_1`×1；每日宝箱02 `3801`×1；存钱罐 `3101`×1；降阶碎片×1 | 2.8485607009 | 285 |
| 6 | `test2_3_1_6` | `event_bundle_13` | $9.99 | 宝石×550；体力×200；普通自选箱02-阶段1 `ns_2_1`×2；普通自选箱04-阶段1 `ns_4_1`×1；资源礼包D01 `rc_d_1`×1；每日宝箱01 `3701`×1；存钱罐 `3101`×2；降阶碎片×2 | 2.6486486486 | 265 |

## 自选箱的可选内容

下列是 `ItemModelConfig.json` 中的 `Choices`，礼包发的是箱子，玩家打开时从对应列表中选择，而不是同时获得列表内全部物品。

| 物品编码 | 配置名称 | 可选内容 |
|---|---|---|
| `4101` | 普通自选宝箱 | 果酱三明治 `207`；甜香混合咖啡 `507`；虾仁意面 `905` |
| `ns_2_1` | 普通自选箱02-阶段1 | 半份火腿三明治 `208`；咖啡师精选 `508`；海鲜意面 `906` |
| `ns_3_1` | 普通自选箱03-阶段1 | 闪亮泳圈 `1305`；潜水手套 `1404`；金色海螺 `1805` |
| `ns_4_1` | 普通自选箱04-阶段1 | 美人鱼泳圈 `1306`；潜水脚蹼 `1405`；稀有鹦鹉螺 `1806` |
| `os_1_1` | 订单自选箱01-阶段1 | 黄油果酱吐司 `206`；番茄酱薯条 `304`；混合咖啡 `506`；乡村冰茶 `604`；双人烤鱼 `904`；橙味纸杯蛋糕 `1104`；粉色果冻圈 `1304`；基础救生衣 `1403`；斑点宝螺 `1804` |

## 每日宝箱内容池

每日宝箱不是固定给出一件物品；下面列出配置中的产出池和权重。权重是配置值，不应当当作百分比。

### `3701`：每日宝箱01

| 物品 | 权重 | 物品 | 权重 | 物品 | 权重 |
|---|---:|---|---:|---|---:|
| 发酵面团 `202` | 6 | 外带杯 `502` | 6 | 蜜汁三文鱼 `902` | 6 |
| 初学者的面包 `203` | 10 | 即饮杯 `503` | 10 | 整条烤鱼 `903` | 10 |
| 酸种面包 `204` | 10 | 无咖啡因咖啡 `504` | 10 | 双人烤鱼 `904` | 10 |
| 切片面包 `205` | 7 | 普通咖啡 `505` | 7 | 虾仁意面 `905` | 7 |

### `3801`：每日宝箱02

| 物品 | 权重 | 物品 | 权重 | 物品 | 权重 |
|---|---:|---|---:|---|---:|
| 橘子切片 `1102` | 6 | 花式小鸭 `1302` | 6 | 基础扇贝 `1802` | 6 |
| 橘子味饼干 `1103` | 10 | 救生圈 `1303` | 10 | 鲜彩扇贝 `1803` | 10 |
| 橙味纸杯蛋糕 `1104` | 10 | 粉色果冻圈 `1304` | 10 | 斑点宝螺 `1804` | 10 |
| 温馨橙子蛋糕 `1105` | 7 | 闪亮泳圈 `1305` | 7 | 金色海螺 `1805` | 7 |

## 资源礼包和存钱罐的内容池

`rc_a_1` 至 `rc_d_1` 是打开后随机掉落资源的礼包；以下为配置中的物品-权重池。金币、宝石和能量类物品的中文名按 `ItemTypeNameMap_zh.json` 对照。

| 资源礼包 | 随机内容（物品编码-权重） | 配置最大数量 |
|---|---|---:|
| `rc_a_1` 资源礼包A01 | 一枚金币 `2401`-3；两枚金币 `2402`-1；能量火花 `2601`-3；能量爆发 `2602`-1 | 8 |
| `rc_b_1` 资源礼包B01 | 一枚金币 `2401`-2；两枚金币 `2402`-2；单颗宝石 `2501`-2；能量火花 `2601`-2；能量爆发 `2602`-1；能量冲击 `2603`-1 | 10 |
| `rc_c_1` 资源礼包C01 | 一枚金币 `2401`-2；两枚金币 `2402`-2；零散金币 `2403`-1；单颗宝石 `2501`-2；两颗宝石 `2502`-1；能量火花 `2601`-2；能量爆发 `2602`-1；能量冲击 `2603`-2 | 13 |
| `rc_d_1` 资源礼包D01 | 一枚金币 `2401`-2；两枚金币 `2402`-2；零散金币 `2403`-2；单颗宝石 `2501`-2；两颗宝石 `2502`-1；几颗宝石 `2503`-1；能量爆发 `2602`-2；能量冲击 `2603`-2；能量震击 `2604`-1 | 15 |

`3101` 存钱罐的随机池为：一枚金币 `2401`-40、两枚金币 `2402`-30、零散金币 `2403`-20、一摞金币 `2404`-10；最大数量和存储上限均为 5。

`3701` 和 `3801` 的产出池最大数量、存储上限均为 10。上述随机池只说明“可能产出什么”和配置权重；服务器没有在等级礼包配置中进一步固定实际开出的单件物品。

## 数据来源与核对

- 礼包内容：`分析内容/server_dump/server_dump/20260728_215244/gh_server_configs_live.json` 的 `levelupGift.config[0]`。
- 历史一致性：目录内 `20260422_001529` 至 `20260728_215244` 的 18 份 `gh_server_configs_live.json`（含重复备份）均为 `groupID=23`、等级字段 30–60、同一套 6 档内容。
- 礼包触发/轮换逻辑：`gh/output/gossipharbor_3.86.0_lua/lua/Model/Bundle/BundleLevelUpGiftModel.lua`。
- 物品名称与自选/随机池：`配置表/Data/Config/ItemTypeNameMap_zh.json`、`ItemModelConfig.json`。
